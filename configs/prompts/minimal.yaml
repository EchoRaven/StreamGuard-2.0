sink: ''
policy_header: ${policy_text}
event_recap: '已积累的证据(本事件):

  ${evidence}'
frame_marker: '[帧 ${index} @ ${t_s}s]'
task_judge: 看这 ${n_frames} 帧。回 hold/flag/clear。
task_perception: '以下是最近 ${n_frames} 帧。描述其中**可见的内容**:

  主体、动作、场景、可读文字。只陈述看到的,不做任何判断。'
format_judge: '{"action":"...","policy_citation":"..."}'
format_perception: '只输出一个 JSON 对象:

  {"action":"hold","description":"<你看到的内容>"}'
exemplar: '示例 ${index}:

  输入: ${input}

  输出: ${output}'
joiner: '


  '
exemplar_joiner: '


  '
name: minimal
